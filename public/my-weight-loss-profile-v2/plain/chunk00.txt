
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>My Weight Loss Profile</title>
<style>
  :root{
    --navy:#14213d; --blue:#2f5d8a; --teal:#3f7c85; --pale:#eef4f8; --line:#d8e2ea;
    --text:#1f2a37; --muted:#65758b; --good:#eaf6ef; --warn:#fff7e8; --high:#fdecec;
    --card:#ffffff; --bg:#f6f8fb;
  }
  *{box-sizing:border-box}
  body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--text)}
  .shell{max-width:1120px;margin:0 auto;padding:28px 18px 60px}
  .brand{display:flex;align-items:center;gap:18px;margin-bottom:24px;flex-wrap:wrap}
  .brand-logo{width:190px;max-width:42vw;height:auto;display:block;object-fit:contain}
  .brand-copy{border-left:1px solid var(--line);padding-left:18px}
  h1{font-size:clamp(1.7rem,4vw,2.5rem);line-height:1.1;margin:0;color:var(--navy)}
  h2{margin:0 0 12px;color:var(--navy)}
  h3{margin:0 0 8px;color:var(--navy)}
  .subtitle{margin:6px 0 0;color:var(--muted)}
  .card{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:22px;box-shadow:0 8px 26px rgba(20,33,61,.05);margin-top:18px}
  .notice{background:var(--pale);border-left:4px solid var(--blue);padding:14px 16px;border-radius:10px;margin:14px 0}
  .progress{height:10px;background:#e7edf2;border-radius:999px;overflow:hidden}
  .progress > div{height:100%;background:linear-gradient(90deg,var(--blue),var(--navy));width:0;transition:width .25s ease}
  .meta{display:flex;justify-content:space-between;gap:12px;margin-top:8px;color:var(--muted);font-size:.9rem}
  .question{font-size:1.28rem;line-height:1.45;margin:20px 0 10px;color:var(--navy);font-weight:750}
  .domain{display:inline-block;font-size:.8rem;font-weight:700;letter-spacing:.03em;text-transform:uppercase;color:var(--blue);background:var(--pale);padding:6px 9px;border-radius:999px}
  .options{display:grid;gap:10px;margin-top:18px}
  .option{display:flex;align-items:center;gap:12px;border:1px solid var(--line);border-radius:12px;padding:14px;cursor:pointer;transition:.15s}
  .option:hover{border-color:var(--blue);background:#fafcfe}
  .option input{width:18px;height:18px}
  .option.selected{border-color:var(--blue);background:var(--pale)}
  .actions{display:flex;justify-content:space-between;gap:12px;margin-top:22px}
  button{border:0;border-radius:11px;padding:12px 18px;font-weight:750;cursor:pointer}
  .primary{background:var(--navy);color:white}
  .secondary{background:#e9eef3;color:var(--navy)}
  button:disabled{opacity:.45;cursor:not-allowed}
  .hidden{display:none!important}
  .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:14px}
  .grid-2{display:grid;grid-template-columns:1.15fr 1fr;gap:18px}
  label.field{display:grid;gap:7px;font-weight:650;color:var(--navy)}
  input[type="number"], input[type="text"], select, textarea{
    width:100%;border:1px solid var(--line);border-radius:10px;padding:11px 12px;font:inherit;background:white
  }
  textarea{min-height:90px;resize:vertical}
  .result-card{border:1px solid var(--line);border-radius:15px;padding:16px;background:white}
  .score{font-size:2rem;font-weight:850;color:var(--navy)}
  .band{font-weight:800}
  .low{background:var(--good)}
  .moderate{background:var(--warn)}
  .high{background:var(--high)}
  .badge{display:inline-block;padding:5px 8px;border-radius:999px;font-size:.78rem;font-weight:750;margin-top:6px;background:#f1f5f8}
  .result-card ul{padding-left:20px;margin-bottom:0}
  .fine{font-size:.86rem;color:var(--muted);line-height:1.45}
  .comparison-row{display:grid;grid-template-columns:1.25fr repeat(4,1fr);gap:6px;align-items:center;padding:8px 0;border-bottom:1px solid #edf1f4}
  .comparison-row label{font-size:.83rem;text-align:center}
  .comparison-row .row-title{font-weight:700;color:var(--navy);text-align:left}
  .treatment-card{border:1px solid var(--line);border-radius:15px;padding:16px;background:white}
  .treatment-card h4{margin:0 0 8px;color:var(--navy)}
  .pill{display:inline-block;padding:4px 8px;border-radius:999px;background:var(--pale);color:var(--navy);font-size:.78rem;font-weight:700;margin:2px 4px 0 0}
  .quad-wrap{background:#fff;border:1px solid var(--line);border-radius:18px;padding:16px}
  .quad-title{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}
  .quad-caption{font-size:.88rem;color:var(--muted)}
  .legend{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px;font-size:.85rem;color:var(--muted)}
  .legend span{display:inline-flex;align-items:center;gap:6px}
  .swatch{width:12px;height:12px;border-radius:50%}
  .small-note{font-size:.8rem;color:var(--muted);margin-top:8px}
  .priority-box{padding:14px;border:1px dashed #bfd0dc;border-radius:14px;background:#fbfdfe}
  .list-tight{margin:0;padding-left:18px}
  .list-tight li{margin:4px 0}
  .axis-key{display:flex;justify-content:space-between;font-size:.82rem;color:var(--muted);margin-top:6px}
  .metric-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(145px,1fr));gap:12px;margin-top:14px}
  .metric-card{border:1px solid var(--line);border-radius:14px;padding:14px;background:#fff}
  .metric-label{font-size:.78rem;text-transform:uppercase;letter-spacing:.04em;color:var(--muted);font-weight:750}
  .metric-value{font-size:1.55rem;color:var(--navy);font-weight:850;margin-top:4px}
  .metric-unit{font-size:.8rem;color:var(--muted);font-weight: